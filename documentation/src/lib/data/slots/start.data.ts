import type { ComponentSlotAPI } from '$lib/types/component-data-api.type';

export const start: ComponentSlotAPI = {
	description: {
		en: `Content is placed to the left of the button text in LTR, and to the right in RTL.`
	},
	name: 'start'
};
