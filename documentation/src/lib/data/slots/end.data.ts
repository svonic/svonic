import type { ComponentSlotAPI } from '$lib/types/component-data-api.type';

export const end: ComponentSlotAPI = {
	description: {
		en: `Content is placed to the right of the button text in LTR, and to the left in RTL.`
	},
	name: 'end'
};
